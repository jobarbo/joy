// ==========================================================================
// INITIALIZATION
// ==========================================================================

//
//
// DEFINE MODULE
define(
  [
    'jquery'
  ],
  function($) {

    // This modules is used EXCLUSIVELY for the initialization of scripts that affect the whole site, or script that only require to be initialized and nothing else
    // Ex.: script to change the appearance of <select>

  }
);
